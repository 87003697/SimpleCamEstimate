# V2M4相机搜索算法 - 依赖文件 (更新版本)

# 核心深度学习库
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
scipy>=1.7.0

# 图像处理
opencv-python>=4.5.0
Pillow>=8.0.0
scikit-image>=0.18.0

# StableNormal相关依赖 (升级版本)
diffusers==0.34.0          # ⬆️ 升级自 0.25.1
transformers==4.53.1       # ⬆️ 升级自 4.36.0
huggingface_hub==0.33.2    # ⬆️ 升级自 0.23.0
tokenizers==0.21.2         # ⬆️ 升级自 0.15.2
peft==0.16.0               # ⬆️ 升级自 0.13.2
accelerate==1.8.1          # ⬆️ 升级自 1.1.1
xformers==0.0.31.post1     # ⬆️ 升级自 0.0.28.post1

# 渲染和可视化
kiui>=0.2.0                # 高质量渲染 (必需)
nvdiffrast                 # GPU加速渲染 (必需)
matplotlib>=3.5.0
tqdm>=4.60.0

# 数据处理
safetensors>=0.3.0

# 注意事项:
# 1. kiui和nvdiffrast是核心依赖，缺少会导致算法无法运行
# 2. StableNormal需要最新版本的diffusers和transformers
# 3. 建议使用conda环境进行安装
# 4. 如果遇到xformers问题，可以单独安装：pip install xformers --index-url https://download.pytorch.org/whl/cu118

# 安装命令:
# pip install -r requirements.txt
# 或者分步安装以避免冲突:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# pip install -r requirements.txt 